{
  "name": "events",
  "type": "module",
  "private": true,
  "version": "1.0.0",
  "sideEffects": false,
  "scripts": {
    "build": "tsdown",
    "typecheck": "tsc --noEmit",
    "lint": "biome lint --write .",
    "deploy": "bun run build && bun run zip",
    "format": "biome check --write .",
    "clean": "rm -rf dist .turbo lambda.zip",
    "zip": "zip -j lambda.zip dist/index.cjs"
  },
  "dependencies": {
    "@app/database": "workspace:*"
  },
  "devDependencies": {
    "tsdown": "catalog:",
    "typescript": "catalog:",
    "@types/node": "catalog:",
    "@app/tsconfig": "workspace:*"
  },
  "engines": {
    "node": ">=22"
  }
}
